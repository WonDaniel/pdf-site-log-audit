[cron:<batch-name>] Use the pdf-site-log-audit skill. Read <batch-task-json>. Pick exactly one pending file. Determine whether it is digital-native or scanned. If scanned: copy the source PDF into task_dir, run scripts/ocr_extract.py on the local task copy, persist OCR artifacts, then run scripts/build_page_mapping.py, then run scripts/find_duplicate_candidates.py, then perform agent duplicate confirmation and severe text-image mismatch review, then write/update the final 分析报告.docx beside the source PDF using the approved template. Update the task status stage-by-stage (`prepared`, `ocr_done`, `mapping_done`, `candidate_done`, `review_done`, `report_done`, `completed` or `failed`) and stop after exactly one file. Do not replace the OCR step with ad-hoc model OCR or direct PDF-tool prompting.
